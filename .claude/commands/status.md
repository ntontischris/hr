Give me a quick status overview of the project:

1. **Git Status**: Run `git status` and summarize
   - Current branch
   - Uncommitted changes (count)
   - Files ahead/behind remote

2. **Recent Activity**: Run `git log --oneline -10` 
   - Show last 10 commits

3. **Health Check**: 
   - Run `pnpm build` — does it compile?
   - Run `pnpm lint` — any lint errors?
   - Run `pnpm test` — tests passing?

4. **Summary**:
   - 🟢 All good / 🟡 Minor issues / 🔴 Needs attention
   - List any actionable items
